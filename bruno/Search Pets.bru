meta {
  name: Search Pets (User)
  type: http
  seq: 6
}

get {
  url: {{api_url}}/api/pets/search?name=Fluffy
  body: none
  auth: bearer
}

auth:bearer {
  token: {{user_token}}
}

assert {
  res.status: eq 200
}
